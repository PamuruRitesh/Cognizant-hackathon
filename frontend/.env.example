# Frontend environment configuration
# Copy this file to .env and set VITE_API_BASE to your backend URL.
# Vite only exposes variables prefixed with VITE_ to the browser bundle.

VITE_API_BASE=http://localhost:8000
